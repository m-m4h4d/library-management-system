<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-gray-100 font-sans min-h-screen">
    <!-- Header Section -->
    <header class="bg-blue-600 text-white py-6 shadow-md">
      <div class="container mx-auto px-4 flex justify-between items-center">
        <h1 class="text-3xl font-bold">Library Management System</h1>
        <nav>
          <a href="/" class="text-white text-lg mx-4 hover:text-blue-200"
            >Home</a
          >
          <a href="/logout" class="text-white text-lg mx-4 hover:text-blue-200"
            >Logout</a
          >
        </nav>
      </div>
    </header>

    <!-- Main Section -->
    <main class="flex items-center justify-center bg-gray-100">
      <div
        class="bg-white p-6 rounded-lg shadow-lg max-w-md w-full text-center"
      >
        <h2 class="text-2xl font-semibold text-gray-700 mb-6">
          Welcome, <%= user.name %> <br />(<%= user.email %>)
        </h2>
        <div class="space-y-4">
          <% if (user.role === 'admin') { %>
          <a
            href="/books/add"
            class="block w-full py-2 px-4 text-white bg-blue-500 rounded-md hover:bg-blue-600 transition"
          >
            Add New Book
          </a>
          <% } %> <% if (user.role === 'member') { %>
          <a
            href="/books/borrowed"
            class="block w-full py-2 px-4 text-white bg-green-500 rounded-md hover:bg-green-600 transition"
          >
            Your Borrowed Books
          </a>
          <% } %>
          <a
            href="/books"
            class="block w-full py-2 px-4 text-white bg-gray-500 rounded-md hover:bg-gray-600 transition"
          >
            View Books
          </a>
        </div>
      </div>
    </main>
  </body>
</html>
